-- -- JDTLS configuración con variables de entorno explícitas
-- local home = os.getenv("HOME")
-- local default_java_home = "/Library/Java/JavaVirtualMachines/zulu-21.jdk/Contents/Home"
--
-- -- Usar JAVA_HOME definido o usar el valor por defecto
-- local java_home = os.getenv("JAVA_HOME") or default_java_home
-- local java_binary = java_home .. "/bin/java"
--
-- -- Rutas importantes
-- local jdtls_path = home .. "/workspaces/utils/jdt-language-server-1.52.0-202510230337"
-- local lombok_jar = home .. "/workspaces/utils/lombok.jar"
-- local project_name = vim.fn.fnamemodify(vim.fn.getcwd(), ':p:h:t')
-- local workspace_dir = vim.fn.stdpath('data') .. '/jdtls-workspace/' .. project_name
-- local config_dir = jdtls_path .. "/config_mac"
--
-- -- Buscar launcher jar
-- local launcher_glob = jdtls_path .. "/plugins/org.eclipse.equinox.launcher_*.jar"
-- local launcher_jar = vim.fn.glob(launcher_glob, true)
--
-- -- Establecer variables de entorno críticas para JNI
-- local env = {
--     -- Variables críticas para JNI
--     PATH = home .. "/bin:" ..
--            home .. "/.local/bin:" ..
--            "/usr/local/bin:" ..
--            "/usr/bin:" ..
--            "/bin:" ..
--            "/usr/sbin:" ..
--            "/sbin:" ..
--            java_home .. "/bin",
--     JAVA_HOME = java_home,
--     -- Variables específicas para macOS y bibliotecas nativas
--     DYLD_LIBRARY_PATH = java_home .. "/lib:" .. java_home .. "/lib/server",
-- }
--
-- -- Java command con configuración para entorno macOS
-- local java_cmd = {
--     java_binary,
--     -- JNI y natives
--     "-Djava.library.path=" .. java_home .. "/lib:" .. java_home .. "/lib/server",
--     "-Djava.awt.headless=true",
--     -- Suprime advertencias de módulos incubator
--     "-XX:+UnlockDiagnosticVMOptions",
--     "-XX:+IgnoreUnrecognizedVMOptions",
--     "-Djdk.module.showModuleResolution=false",
--     -- Módulos Java para permisos
--     "--add-modules=ALL-SYSTEM",
--     "--add-opens", "java.base/java.util=ALL-UNNAMED",
--     "--add-opens", "java.base/java.lang=ALL-UNNAMED",
--     -- Lombok (comentado inicialmente)
--     -- "-javaagent:" .. lombok_jar,
--     -- JDTLS esencial
--     "-Declipse.application=org.eclipse.jdt.ls.core.id1",
--     "-Dosgi.bundles.defaultStartLevel=4",
--     "-Declipse.product=org.eclipse.jdt.ls.core.product",
--     "-Dlog.protocol=true",
--     "-Dlog.level=ALL",
--     -- Memoria
--     "-Xms256m",
--     "-Xmx512m",
--     -- Archivo JAR, configuración y datos
--     "-jar", launcher_jar,
--     "-configuration", config_dir,
--     "-data", workspace_dir
-- }
--
-- return {
--     'mfussenegger/nvim-jdtls',
--     config = function()
--         -- Limpiar workspace al inicio
--         local ws_dir = vim.fn.stdpath('data') .. '/jdtls-workspace/' .. project_name
--         if vim.fn.isdirectory(ws_dir) == 1 then
--             local cmd = "rm -rf " .. vim.fn.shellescape(ws_dir)
--             os.execute(cmd)
--             vim.notify("Workspace limpiado automáticamente en inicio", vim.log.levels.INFO)
--         end
--
--         -- Limpiar logs antiguos
--         local lsp_log = vim.fn.stdpath('state') .. '/lsp.log'
--         if vim.fn.filereadable(lsp_log) == 1 then
--             os.execute("echo '' > " .. vim.fn.shellescape(lsp_log))
--             vim.notify("Log LSP limpiado", vim.log.levels.INFO)
--         end
--
--         -- Comando para cargar variables de entorno desde zsh
--         vim.api.nvim_create_user_command("JdtLoadEnv", function()
--             -- Intentar cargar variables desde zsh
--             local handle = io.popen("zsh -c 'source ~/.zshrc && env'")
--             if handle then
--                 local result = handle:read("*a")
--                 handle:close()
--
--                 -- Extraer PATH y otras variables críticas
--                 local zsh_path = result:match("PATH=([^\n]+)")
--                 local zsh_java_home = result:match("JAVA_HOME=([^\n]+)")
--
--                 if zsh_path then
--                     vim.notify("PATH from zsh: " .. zsh_path, vim.log.levels.INFO)
--                     env.PATH = zsh_path
--                 end
--
--                 if zsh_java_home then
--                     vim.notify("JAVA_HOME from zsh: " .. zsh_java_home, vim.log.levels.INFO)
--                     env.JAVA_HOME = zsh_java_home
--                 end
--
--                 vim.notify("Variables de entorno actualizadas desde zsh", vim.log.levels.INFO)
--             else
--                 vim.notify("No se pudieron cargar variables de zsh", vim.log.levels.ERROR)
--             end
--         end, {})
--
--         vim.api.nvim_create_autocmd({"FileType"}, {
--             pattern = {"java"},
--             callback = function()
--                 -- Solo para archivos Java
--                 local filename = vim.api.nvim_buf_get_name(0)
--                 if not filename:match("%.java$") then
--                     return
--                 end
--
--                 -- Intentar iniciar JDTLS
--                 local status, jdtls = pcall(require, 'jdtls')
--                 if not status then
--                     vim.notify("No se pudo cargar jdtls", vim.log.levels.ERROR)
--                     return
--                 end
--
--                 -- Mostrar variables de entorno
--                 vim.notify("Usando JAVA_HOME: " .. env.JAVA_HOME, vim.log.levels.INFO)
--                 vim.notify("PATH: " .. env.PATH, vim.log.levels.INFO)
--
--                 -- Configuración con variables de entorno
--                 local config = {
--                     cmd = java_cmd,
--                     root_dir = require('jdtls.setup').find_root({'pom.xml', 'build.gradle', '.git'}),
--                     settings = {
--                         java = {
--                             configuration = {
--                                 updateBuildConfiguration = "automatic",
--                                 runtimes = {{
--                                     name = "JavaSE-21",
--                                     path = env.JAVA_HOME,
--                                     default = true
--                                 }},
--                             },
--                             errors = {
--                                 incompleteClasspath = { severity = "ignore" }
--                             },
--                             trace = { server = "verbose" }
--                         }
--                     },
--                     init_options = {
--                         bundles = {}
--                     },
--                     -- Agregar las variables de entorno explícitas
--                     cmd_env = env
--                 }
--
--                 -- Iniciar servidor con entorno adecuado
--                 jdtls.start_or_attach(config)
--                 vim.notify("JDTLS iniciado con variables de entorno explícitas", vim.log.levels.INFO)
--             end
--         })
--
--         -- Comando para habilitar Lombok una vez que funciona el entorno
--         vim.api.nvim_create_user_command("JdtEnableLombok", function()
--             if vim.fn.filereadable(lombok_jar) ~= 1 then
--                 vim.notify("❌ Lombok JAR no encontrado: " .. lombok_jar, vim.log.levels.ERROR)
--                 return
--             end
--
--             -- Detener JDTLS actual
--             pcall(function() vim.cmd("LspStop jdtls") end)
--             vim.notify("JDTLS detenido para reconfigurarlo con Lombok", vim.log.levels.INFO)
--
--             -- Añadir la opción de Lombok
--             table.insert(java_cmd, 3, "-javaagent:" .. lombok_jar)
--
--             -- Configuración de Java actualizada
--             vim.notify("Configuración actualizada para incluir Lombok", vim.log.levels.INFO)
--             vim.notify("Por favor abre un archivo Java para iniciar JDTLS con Lombok", vim.log.levels.INFO)
--         end, {})
--
--         -- Comando para mostrar información del entorno
--         vim.api.nvim_create_user_command("JdtEnvInfo", function()
--             vim.notify("Environment Variables in Neovim:", vim.log.levels.INFO)
--             vim.notify("PATH: " .. (env.PATH or "not set"), vim.log.levels.INFO)
--             vim.notify("JAVA_HOME: " .. (env.JAVA_HOME or "not set"), vim.log.levels.INFO)
--             vim.notify("DYLD_LIBRARY_PATH: " .. (env.DYLD_LIBRARY_PATH or "not set"), vim.log.levels.INFO)
--
--             -- Verificar archivos críticos
--             if vim.fn.executable(java_binary) == 1 then
--                 vim.notify("✅ Java binary exists: " .. java_binary, vim.log.levels.INFO)
--             else
--                 vim.notify("❌ Java binary not found: " .. java_binary, vim.log.levels.ERROR)
--             end
--
--             if vim.fn.filereadable(launcher_jar) == 1 then
--                 vim.notify("✅ Launcher jar exists: " .. launcher_jar, vim.log.levels.INFO)
--             else
--                 vim.notify("❌ Launcher jar not found: " .. launcher_jar, vim.log.levels.ERROR)
--             end
--
--             -- Intentar ejecutar java para verificar JNI
--             local cmd = env.JAVA_HOME .. "/bin/java -version 2>&1"
--             local handle = io.popen(cmd)
--             if handle then
--                 local result = handle:read("*a")
--                 handle:close()
--                 vim.notify("Java version check: " .. result, vim.log.levels.INFO)
--             end
--         end, {})
--     end
-- }
return {}
